# Supabase — get these from supabase.com → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...   # JWT format (not sb_publishable_)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...        # server/worker only, never expose to client
AGENT_NEWS_INGEST_TOKEN=replace-with-long-random-secret

# Claude API — for news classification worker (Stage 4)
ANTHROPIC_API_KEY=sk-ant-...

# News ingestion — optional, for Stage 3 worker
# DIFFBOT_TOKEN=...
# NEWSAPI_KEY=...
